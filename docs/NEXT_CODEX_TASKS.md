# Next Codex Tasks

Stato operativo corrente: iniziativa `GUI-U - Caronte Desktop utente` aperta in fase di governance iniziale. La GUI di `V114-T17` e` chiusa come `CLOSED_AS_TECHNICAL_PROTOTYPE`, resta uno strumento tecnico e non e` la GUI destinata all'utente finale.

## Prossimo task

- `GUI-U-B01 - Backlog operativo e Definition of Done`.

Esito atteso: predisporre in una run documentale separata il backlog operativo dettagliato
di GUI-U e la relativa Definition of Done, con task misurabili, dipendenze, criteri di
accettazione e gate umani espliciti.

Nessun task di codice e` ancora attivo. Non iniziare `GUI-U-E0-T01` e non ampliare la GUI tecnica.

La sequenza completa resta in `docs/DEV_BACKLOG.md`.
